// Time allowing this will be a page to display the search history of the user
// theres a better way to do this.... less work I mean
// made the search history into a version of the results page, called by a button found on the search page
import React from "react";

const SearchHistory = () => {
    return (
    <div>Search History</div>
    )
}

